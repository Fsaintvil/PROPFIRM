# Project inventory (automated scan)

This file is generated by an automated scan and lists important files and directories.

Top-level notable files/folders:

- `notebooks/analysis_notebook.ipynb` (analysis notebook)
- `models/model_example.pkl` (trained model binary)
- `data/backtests/` (backtest CSVs, plots, PDF reports)
- `data/archives/` (dated snapshots with JSONL/CSV)
- `archive/obsolete/` (archived temporary scripts and placeholders)
- `scripts/` (operational scripts and connectors)
- `tests/` (pytest test-suite)
- `.github/workflows/` (CI workflows: `ci.yml`, `pytest.yml`)
- `requirements*.txt`, `pyproject.toml`

Large files detected (candidates for archival or LFS):

- `models/model_example.pkl` (binary model)
- `data/backtests/backtest_report.pdf` (PDF)
- `data/backtests/*.csv` (historical datasets, multiple CSVs)
- `archive/obsolete/large_files/analysis_notebook.ipynb`

Notes:
- Consider moving large binary files (models, datasets, PDFs) to `archive/obsolete/large_files/` and then add to `.gitignore` or use Git LFS.
- `MetaTrader5` is not importable in the current environment due to NumPy ABI issues; see `scripts/mt5_connector.py` and `SETUP.md` for guidance.

Timestamp: 2025-10-13T09:45:00Z
