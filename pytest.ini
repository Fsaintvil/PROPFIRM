[pytest]
# Exclude generated preview/proposed patches and other noisy dirs from collection
norecursedirs = */archive venv* .git patches patches/* patches/merged_preview patches/proposed
testpaths = tests
addopts = -q
